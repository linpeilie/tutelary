spring:
  h2:
    console:
      path: /h2-console
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/tutelary/db
    username: root
    password:
    sql-script-encoding: UTF-8
    schema: classpath:db/schema.sql
    initialization-mode: always